/**
 * @sefer/sdk — the client for talking to a Sofer.
 *
 * Inscribe a shaliach, resolve a shem, or discover agents by intent — always with local
 * chotam verification, so the registry is never a trusted party.
 */
export * from "./client";
export * from "./errors";
